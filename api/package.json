{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "description": "The Wall - Azure Functions API",
  "engines": {
    "node": "22.x"
  },
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@azure/cosmos": "^4.2.0",
    "@azure/functions": "^4.6.1"
  },
  "devDependencies": {
    "@types/node": "22.x",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
